// Shape of ClasseController::allVpClasses' rows - a classe currently assigned to a given VP
// (censeur) in the current section+year.
export interface VpClasse {
  classe_id: number;
  classe_name: string;
  level: number;
}
