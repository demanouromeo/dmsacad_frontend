# Loaded by `vite build`/`vite preview` (mode=production). Not a secret - the deployed API's
# public base URL - so this is committed, unlike .env.local.
VITE_BASE_REMOTE_URL=https://dmsacad.com/dmsacad_backend_secured/
