# Copy to .env.local and adjust for your own machine - see MyConstants.tsx and
# frontend/CLAUDE.md's "Backend base URL" section. .env.local is git-ignored (*.local);
# VITE_BASE_REMOTE_URL normally only needs setting in .env.production, not here.
VITE_BASE_LOCAL_URL=http://localhost/dmsacad-react/backend/
